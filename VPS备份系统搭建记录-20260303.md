# VPS备份系统搭建 - 2026-03-03

## 📋 目标
为Hostinger VPS和本地OpenClaw工作区搭建完整的GitHub备份系统，实现自动备份和一键恢复。

## 🎯 完成状态
- ✅ VPS备份配置完成
- ✅ 本地OpenClaw备份配置完成
- ✅ 自动备份任务配置完成

---

## 🖥️ VPS备份配置

### 服务器信息
- **IP地址**：76.13.219.143
- **主机名**：srv1437164.hstgr.cloud
- **用户**：root
- **备份目录**：/vps-backup
- **备份脚本**：/root/vps-backup-v4.sh

### GitHub仓库
- **仓库地址**：https://github.com/169068671/vps-backup
- **克隆地址**：git@github.com:169068671/vps-backup.git
- **SSH密钥**：已配置 (root/.ssh/id_ed25519)

### 备份内容

#### 1. 系统配置 (`etc-config/`)
- `sshd_config` - SSH服务器配置
- `hosts` - 主机名解析
- `hostname` - 服务器主机名
- `fstab` - 文件系统挂载配置

#### 2. Root配置 (`root-config/`)

**Docker配置：**
- `guacamole-docker-compose.yml` - Guacamole Docker编排文件
- `guacamole-docker-compose.yml.backup` - 备份版本
- `init.sql` - 数据库初始化脚本
- `guacamole-backup-20260303-015124.sql` - MySQL数据库备份

**OpenClaw配置：**
- `openclaw.json` - OpenClaw主配置
- `openclaw-config.json` / `openclaw-config-corrected.json` / `openclaw-config-final.json` - 配置版本

**Whisper语音识别：**
- `whisper-docker-compose.yml`
- `whisper-python-compose.yml`
- `whisper-rotemb-compose.yml`
- `whisper-simple-compose.yml`
- `whisper-deploy.sh`
- `whisper-test-cpu.sh`
- `whisper-test-script.sh`
- `whisper-transcribe.sh`

**备份脚本：**
- `vps-backup-fixed.sh`
- `vps-backup-full.sh`
- `vps-backup-script.sh`
- `vps-backup-v3.sh`
- `vps-backup-v4.sh` (当前使用)

**其他脚本：**
- `kejilion.sh` - 服务器管理脚本

**SSH安全：**
- ✅ `authorized_keys` - SSH公钥（用于登录）
- ❌ 已排除：`id_ed25519` (私钥)、`known_hosts` (敏感)

**VNC配置：**
- `xstartup` - VNC启动脚本
- `srv1437164.hstgr.cloud:5901.pid` - VNC进程ID

**其他配置：**
- `.bashrc` - Bash环境配置
- `.profile` - 用户环境配置
- `.gitconfig` - Git配置
- `openclaw-gateway.service` - OpenClaw系统服务
- XFCE桌面配置文件

#### 3. 系统信息 (`system-info/`)
- `uptime.txt` - 系统运行时间
- `disk-usage.txt` - 磁盘使用情况
- `memory.txt` - 内存使用情况
- `kernel.txt` - 内核版本

### 已排除的敏感文件
- SSH私钥 (`id_ed25519`, `id_rsa`)
- VNC密码文件 (`.vnc/passwd`)
- npm缓存 (`_cacache`)
- node_modules
- `.openclaw/sessions/` (对话历史)
- 大文件 (>50MB)

### 自动备份配置
```bash
# Cron任务 - 每天03:00自动备份
0 3 * * * /root/vps-backup-v4.sh >> /var/log/vps-backup.log 2>&1
```

### 备份脚本
```bash
#!/bin/bash
# VPS 完整备份脚本（v4 - 排除敏感文件）
BACKUP_DIR="/vps-backup"
DATE=$(date +%Y-%m-%d_%H-%M-%S)

cd $BACKUP_DIR || exit 1
mkdir -p {root-config,etc-config,docker-volumes,system-info}

# 备份 root 目录（排除大文件、缓存和敏感文件）
rsync -av --exclude='.cache' --exclude='*.log' --exclude='*.tmp' \
  --exclude='.openclaw' \
  --exclude='.npm/_cacache' \
  --exclude='.npm/_logs' \
  --exclude='node_modules' \
  --exclude='*.tar.gz' \
  --exclude='*.zip' \
  --exclude='.ssh/id_*' \
  --exclude='.ssh/known_hosts' \
  --exclude='.vnc/passwd' \
  /root/ root-config/ 2>/dev/null

# 备份系统配置
cp /etc/ssh/sshd_config etc-config/ 2>/dev/null
cp /etc/hosts etc-config/ 2>/dev/null
cp /etc/hostname etc-config/ 2>/dev/null
cp /etc/fstab etc-config/ 2>/dev/null

# 备份系统信息
uptime > system-info/uptime.txt 2>/dev/null
df -h > system-info/disk-usage.txt 2>/dev/null
free -h > system-info/memory.txt 2>/dev/null
uname -a > system-info/kernel.txt 2>/dev/null

# Git操作
git add .
git commit -m "VPS backup - $DATE"
git push origin main
```

---

## 💻 本地OpenClaw备份配置

### 工作区信息
- **工作区路径**：/home/admin/openclaw/workspace
- **备份脚本**：/home/admin/openclaw/backup-all.sh
- **配置目录**：/home/admin/.openclaw/

### GitHub仓库
- **仓库地址**：https://github.com/169068671/openclaw-backup
- **克隆地址**：git@github.com:169068671/openclaw-backup.git
- **SSH密钥**：已配置 (/home/admin/.ssh/id_ed25519)

### 备份内容
- ✅ OpenClaw配置文件 (openclaw.json, exec-approvals.json, update-check.json)
- ✅ 扩展插件 (dingtalk-connector, feishu, qqbot)
- ✅ 9个技能模块 (amap-traffic, hn, otaku-reco, otaku-wiki, pptx-creator, qwen-image, stock-watcher, url-digest, video-frames)
- ✅ Agent配置（排除sessions）
- ✅ Cron任务
- ✅ 所有工作区文件和文档

### 已排除的文件
- sessions/ (对话历史)
- node_modules/
- *.tar.gz
- *.zip

### 自动备份配置
```bash
# Cron任务 - 每天03:00自动备份
0 3 * * * /home/admin/openclaw/backup-all.sh
```

---

## 🔄 恢复方法

### VPS恢复
```bash
# 克隆备份仓库
git clone git@github.com:169068671/vps-backup.git /vps-backup

# 恢复配置文件
cp /vps-backup/root-config/openclaw.json /root/.openclaw/
cp /vps-backup/root-config/guacamole-docker-compose.yml /root/

# 恢复SSH公钥
cp /vps-backup/root-config/.ssh/authorized_keys ~/.ssh/

# 恢复数据库
docker exec -i guacamole-mysql mysql -u guacamole_user -pguacamole_password \
  guacamole_db < /vps-backup/root-config/guacamole-backup-*.sql
```

### 本地OpenClaw恢复
```bash
# 克隆备份仓库
git clone git@github.com:169068671/openclaw-backup.git /tmp/restore

# 恢复配置
cp -r /tmp/restore/.openclaw/* ~/.openclaw/

# 恢复工作区
cp -r /tmp/restore/workspace/* ~/openclaw/workspace/
```

---

## 📊 备份统计

### VPS备份
- **文件总数**：61个
- **备份频率**：每天03:00
- **日志文件**：/var/log/vps-backup.log

### 本地OpenClaw备份
- **文件总数**：48,381个（首次备份）
- **备份频率**：每天03:00
- **工作区文件**：包含所有项目文档

---

## 🔧 故障排查

### 问题1：GitHub文件大小限制
- **问题**：186.12 MB的文件超过100 MB限制
- **解决**：排除npm缓存文件 (`_cacache`)

### 问题2：敏感文件被拦截
- **问题**：SSH私钥被GitHub安全扫描拦截
- **解决**：在备份脚本中排除 `.ssh/id_*` 文件

### 问题3：Git仓库状态异常
- **问题**：`.openclaw/workspace/` 没有提交历史
- **解决**：重新初始化Git仓库并配置SSH

---

## 📝 备注
- 备份脚本已配置排除规则，确保不泄露敏感信息
- 每日自动备份在凌晨03:00执行
- 可通过查看日志文件监控备份状态
- 建议定期检查备份仓库确认备份成功

---

**备份系统搭建完成** 🎉

*记录日期：2026-03-03*
